{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows build debug",
            "type": "shell",
            "command": "${workspaceFolder}/buildWindows.bat Debug"
        },
        {
            "label": "Windows build release",
            "type": "shell",
            "command": "${workspaceFolder}/buildWindows.bat Release"
        },
        {
            "label": "Linux build debug",
            "type": "shell",
            "command": "${workspaceFolder}/buildLinux.sh Debug"
        },
        {
            "label": "Linux build release",
            "type": "shell",
            "command": "${workspaceFolder}/buildLinux.sh Release"
        },
        {
            "label": "Macos build debug",
            "type": "shell",
            "command": "${workspaceFolder}/buildMacos.sh Debug"
        },
        {
            "label": "Macos build release",
            "type": "shell",
            "command": "${workspaceFolder}/buildMacos.sh Release"
        } 
    ]
}